<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/css/style.css">
    <title>Login | StudyQuest</title>
    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: radial-gradient(circle at 50% 50%, #1e1b4b 0%, #0f172a 100%);
        }

        .login-card {
            width: 100%;
            max-width: 400px;
            padding: 3rem;
            background: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            text-align: center;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            background: #0f172a;
            border: 1px solid #334155;
            border-radius: 8px;
            color: white;
        }
    </style>
</head>

<body>
    <div class="login-card">
        <h1 style="color: #6366f1; font-size: 2.5rem; margin-bottom: 1rem;">⚔️ StudyQuest</h1>

        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button class="btn-premium" style="width: 100%; margin-top: 1rem;">Sign In</button>
        </form>

        <p style="margin-top: 1.5rem; color: #94a3b8; font-size: 0.9rem;">
            New here? <a href="/signup" style="color: #6366f1; text-decoration: none; font-weight: bold;">Create an
                account</a>
        </p>
    </div>
</body>

</html>