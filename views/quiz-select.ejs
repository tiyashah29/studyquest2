<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Select Quest | StudyQuest</title>
</head>

<body>
    <div style="display: flex; min-height: 100vh;">
        <nav
            style="width: 280px; padding: 2rem; border-right: 1px solid var(--border); display: flex; flex-direction: column;">
            <h2 style="color: var(--primary); margin-bottom: 3rem;">⚔️ StudyQuest</h2>
            <div style="display: flex; flex-direction: column; gap: 1rem; flex-grow: 1;">
                <a href="/dashboard" class="nav-link">🏠 Dashboard</a>
                <a href="/quiz-select" class="nav-link active">📝 Take a Quiz</a>
                <a href="/logout" class="nav-link" style="margin-top: auto;">Logout</a>
            </div>
        </nav>

        <main style="flex: 1; padding: 3rem;">
            <header style="margin-bottom: 3rem;">
                <h1 style="font-size: 2.5rem; margin-bottom: 0.5rem;">Choose Your Quest</h1>
                <p style="color: var(--text-muted);">Select a language to test your skills and earn XP.</p>
            </header>

            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;">
                <% quizzes.forEach(quiz=> { %>
                    <div class="glass-card"
                        style="display: flex; flex-direction: column; justify-content: space-between; transition: transform 0.3s ease;">
                        <div>
                            <div
                                style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
                                <h3 style="color: var(--primary); font-size: 1.5rem;">
                                    <%= quiz.language %>
                                </h3>
                                <span style="
                                    padding: 4px 12px; 
                                    border-radius: 20px; 
                                    font-size: 0.75rem; 
                                    font-weight: bold;
                                    text-transform: uppercase;
                                    background: <%= quiz.difficulty === 'Hard' ? 'rgba(239, 68, 68, 0.2)' : (quiz.difficulty === 'Medium' ? 'rgba(245, 158, 11, 0.2)' : 'rgba(16, 185, 129, 0.2)') %>;
                                    color: <%= quiz.difficulty === 'Hard' ? '#ef4444' : (quiz.difficulty === 'Medium' ? '#f59e0b' : '#10b981') %>;
                                    border: 1px solid currentColor;
                                ">
                                    <%= quiz.difficulty %>
                                </span>
                            </div>
                            <p style="color: var(--text-muted); margin-bottom: 2rem; line-height: 1.6;">
                                Complete 10 questions to prove your mastery in <%= quiz.language %>.
                                    Keep your eyes on the screen—our anti-cheat is watching!
                            </p>
                        </div>

                        <div
                            style="display: flex; justify-content: space-between; align-items: center; margin-top: auto;">
                            <div>
                                <p style="font-size: 0.8rem; color: var(--text-muted); text-transform: uppercase;">
                                    Reward</p>
                                <p style="color: var(--accent); font-weight: bold; font-size: 1.1rem;">
                                    <%= quiz.xp_reward %> XP
                                </p>
                            </div>
                            <a href="/quiz/<%= quiz.id %>" class="btn-premium">Start Quest</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </main>
    </div>

    <style>
        /* Hover effect for cards */
        .glass-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
            box-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.7);
        }
    </style>
</body>

</html>